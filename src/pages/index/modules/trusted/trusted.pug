extends constants
block content
    section.trusted
        .trusted__container
            h2.trusted__title Trusted by top Youtuber and Production House
            ul.trusted__list
                each item in youtuberIcons
                    li.trusted__item
                        img.trusted__img(src=item.src class=item.icoClass alt='')
